// I'm tempted to export as default the one function to rule them all: `battle()` but
// am going to not do that for now until we see what other things show up in the
// battle module... named only for now.
export {battle} from './battle'
