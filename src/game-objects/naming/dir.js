import * as dataSourceModdables from 'data-source-moddables'

/**
 * List of the types of name groups available.
 *
 * @return {string[]} List of unique names.
 */
export const dir = () => dataSourceModdables.dir('naming')

export default dir
